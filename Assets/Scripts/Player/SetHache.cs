using UnityEngine;

public class SetHache : MonoBehaviour
{
    [SerializeField] GameObject hacheHolder;


    private void Update()
    {
      /*  if(transform.childCount > 0)
        {
            hacheHolder.SetActive(false);
        }
        else
        {
            hacheHolder.SetActive(true);
        }*/
    }
}
